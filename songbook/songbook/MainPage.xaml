<Page
    x:Class="songbook.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:songbook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="SearchControl" Grid.Row="0" PlaceholderText="Search"
                 FontSize="26" />

        <ListBox x:Name="ResultSearchControl"
                 Grid.Row="1" MaxHeight="200"
                 FontSize="26"
                 Visibility="Collapsed"     
                 >
                 
            <ListBox.ItemTemplate>
            <DataTemplate>
                    <TextBlock  Text="{Binding}"/>
            </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <TextBox x:Name="SongTextControl" Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,-579" VerticalAlignment="Stretch" />
    </Grid>
</Page>
